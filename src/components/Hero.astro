<!-- Hero Section -->
<section class="hero" id="home">
    <div class="container">
        <div class="hero-content">
            <span class="hero-tag">Soluciones Logísticas y de Suministro</span>
            <h1>
                Powering Your Supply Chain Across All Industries
                
            </h1>
            <p>
                Lorem ipsum dolor sit amet consectetur, adipisicing elit. Ipsa eligendi magnam a sunt laboriosam magni repudiandae minima id illum pariatur quod excepturi architecto non recusandae rerum esse animi, totam corrupti!
            </p>
            <a href="#contact" class="btn">Contactar Ahora</a>
        </div>
    </div>
</section>
<style lang="scss">
    /* Hero Section */
    .hero {
        background-color: var(--primary);
        color: var(--white);
        padding: 10rem 0 6rem;
        position: relative;
    }

    .hero-content {
        display: flex;
        flex-direction: column;
        align-items: flex-start;
        max-width: 600px;
    }

    .hero-content h1 {
        margin-bottom: 1rem;
        font-size: 3rem;
    }

    .hero-content p {
        margin-bottom: 2rem;
        font-size: 1.2rem;
        opacity: 0.9;
    }

    .hero-tag {
        color: var(--secondary);
        font-weight: 500;
        margin-bottom: 0.5rem;
    }

    .hero-image {
        position: absolute;
        right: 0;
        top: 0;
        width: 50%;
        height: 100%;
        object-fit: cover;
        opacity: 0.8;
    }
</style>
