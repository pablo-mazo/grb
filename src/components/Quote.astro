<!-- Quote Banner -->
<section class="quote-banner">
    <div class="container">
        <div class="quote-text">
            El éxito en la logística no solo es mover productos, es crear conexiones que impulsan el crecimiento de la empresa y la satisfacción del cliente.
        </div>
        <div class="quote-author">- GoldenRatioBrothers Team</div>
    </div>
</section>
<style>
    /* Testimonials */
    .testimonials {
        padding: 5rem 0;
        position: relative;
    }

    .testimonial-wrapper {
        max-width: 800px;
        margin: 0 auto;
        text-align: center;
    }

    .quote {
        font-size: 1.5rem;
        font-style: italic;
        position: relative;
        padding: 2rem;
    }

    .quote::before {
        content: '"';
        font-size: 5rem;
        color: rgba(0, 0, 0, 0.1);
        position: absolute;
        top: -1rem;
        left: 0;
    }

    .quote-banner {
        background-color: var(--secondary);
        padding: 4rem 0;
        text-align: center;
    }

    .quote-text {
        font-size: 1.5rem;
        font-weight: 500;
        font-style: italic;
        max-width: 800px;
        margin: 0 auto;
        color: var(--primary);
    }

    .quote-author {
        margin-top: 1rem;
        font-weight: bold;
    }

    .author {
        display: flex;
        align-items: center;
        justify-content: center;
        margin-top: 2rem;
    }

    .author-image {
        width: 60px;
        height: 60px;
        border-radius: 50%;
        margin-right: 1rem;
        background-color: #ccc;
        overflow: hidden;
    }

    .author-info h4 {
        margin-bottom: 0.25rem;
    }

    .author-company {
        color: var(--secondary);
        font-weight: 500;
    }
</style>
